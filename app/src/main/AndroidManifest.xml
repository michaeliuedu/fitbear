<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.foodies">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar">
        <activity
            android:name="gg.bear.fitness.WeightActivity"
            android:exported="false" />
        <activity
            android:name="gg.bear.fitness.WorkoutActivity"
            android:exported="false"
            android:label="@string/title_activity_workout"
            android:theme="@style/AppTheme.NoActionBar.NoActionBar" />
        <activity android:name="gg.bear.fitness.SettingsActivity" />
        <activity android:name="gg.bear.fitness.FoodItemActivity" />
        <activity android:name="gg.bear.fitness.RegisterActivity" />
        <activity android:name="gg.bear.fitness.LoginActivity" />
        <activity
            android:name="gg.bear.fitness.SignInActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>